# Progress — fdaAgent

> Live project state for fast session pickup. This is the **dynamic** doc; the static
> roadmap is [PLAN.md](PLAN.md), commands/conventions are [AGENTS.md](AGENTS.md), and the
> file map is [PROJECT_INDEX.md](PROJECT_INDEX.md). To avoid drift, this file **links** to
> those rather than repeating them.
> **Maintenance:** at the end of each work session, update *Now / Next up / Blockers*. Keep it short.
> **Parallel rule:** *Next action / Next up* is the shared multi-agent queue. Subagents must
> not rewrite it as only their own task; preserve all parallel items and put per-branch
> details in the PR body/comments.
> Last updated: 2026-07-14

## Goal (end state)
A deployed, demoable agent that answers natural-language questions about FDA drug recalls with **evidence-backed** results — **Path 1**: deterministic NL→SQL analytics (frequencies / trends / distributions, every number from SQL); **Path 2**: hybrid semantic retrieval + validated semantic-count estimates for fuzzy concepts; served via FastAPI + a small UI, with an eval harness. Reproduces an industry-style LLM structuring + retrieval pipeline on 100% public-domain data (portfolio for NA AI/ML roles). Full roadmap in [PLAN.md](PLAN.md).

## Now
- **State:** Path 1 served + containerized. **Path 2.4 semantic validation/counting, Phase 3/4 sidecar foundations, Frontend v2, and the serving-path OpenAI-compatible provider gateway are in place** — `/ask` can route fuzzy concepts through retrieval → LLM yes/no validation → estimated counts with evidence/confidence; chat / structured-output calls can use `LLM_PROVIDER=openrouter`, and query embeddings can use `EMBED_PROVIDER=openrouter` + `openai/text-embedding-3-small` while preserving the stored 1536-d corpus vectors. Retrieval provider failures still degrade explicitly to `retrieval_mode=fts_only` with `embedding_fallback_reason`; zero-hit degraded fallbacks are caveated instead of treated as factual zero semantic results.
- **Frontend status:** desktop chat-content widening merged in PR #33, long distribution/chart labels now wrap with full-label accessibility metadata in PR #35, recall evidence links merged in PR #38, and slow `/ask` calls now show safe frontend-only progress states instead of a frozen `Thinking...` message; independent scroll and mobile layout rules stay intact.
- **Backend-control status:** `/ask` now routes every prompt through an **LLM intent gate** ([src/agent_control.py](src/agent_control.py) `classify_llm`, wired in [src/nl_query.py](src/nl_query.py) `NLEngine.ask`) before building a `QuerySpec` — a language-agnostic classifier returning `in_domain` / `chitchat_meta` / `out_of_domain` / `ambiguous` with a user-language message + suggestions for terminal routes. This **removes** the brittle English+CJK regex rules entirely — the rule-based `classify()`, its `META_PATTERNS` / `OUT_OF_DOMAIN_PATTERNS` / keyword tables, the rule helpers, and the `is_generic_recall_semantic_query` guard were all deleted, so `/ask` now requires the LLM (no rule fallback), consistent with the rest of the pipeline. **Concept normalization is LLM-driven too:** the hardcoded `refine_semantic_query` concept dictionary (regex → canonical concept + aliases, in English + CJK) was deleted — `generate_spec` now emits the canonical `semantic_query` itself (e.g. `药效太强` / "pills too strong" → `superpotent`) plus keyword aliases, and `refine_spec` only does general hygiene (strip / de-dupe aliases). Remaining agent-control polish: safer FTS-degradation messaging so a zero-hit degraded retrieval is not presented as “FDA has no matching recalls.”
- **Exposure v0 status (PR #55 draft, issues #40/#41):** raw FDA `recalling_firm` exposure is implemented on the feature branch as a deterministic SQL leaderboard. It returns raw recall-count ranking plus a visible `raw_severity_v0` formula (`3 * Class I + 2 * Class II + 1 * Class III`), class breakdowns, top taxonomy reason category, and evidence recall numbers. Scope is intentionally honest: raw FDA-name level only, no parent-group normalization, no brand aliases, and no safety verdict. Blocking review fix applied: the pre-router now only catches explicit unfiltered recall/exposure leaderboards, terminal domain-guard decisions win, and filtered/class-specific prompts are handled by the LLM QuerySpec with hard filters preserved.
- **Retrieval-lab branch (#46):** `/hybrid-search` now has a separate debug UI + POST endpoint that exposes safe vector/FTS/RRF metadata, aliases, filters, retrieval mode, fallback reason, timings, top hits, CSV/JSON export, and a dedicated `hybrid_search_log` migration without changing normal `/ask` answers. Review follow-ups: lab filters now validate allowed ops by column kind, reject invalid value types/date ops with HTTP 400 before SQL, enforce string/list/serialized-size caps, and log only normalized capped filters rather than raw unbounded payloads.
- **Taxonomy status:** taxonomy v1 labeling is populated, cleaned up, and wired into `/ask` for exact counts and firm-scoped top-N summaries. `llm:deepseek/deepseek-v4-pro` has 21,909 `recall_label` rows covering all 17,723 drug-enforcement records; labels are produced per distinct reason text (4,390 distinct reasons) and backfilled to records for cheaper, consistent coverage. The old `rules:v1-prefix` rows were deleted (`DELETE 21522`, verified `old_rules_rows=0`). Exact taxonomy-count wiring merged in PR #25; superseded PR #17 is closed. Earlier draft-taxonomy notes were dry-run review aids only; these populated v1 labels are authoritative for current exact counts.
- **Firm-resolution status:** foundation (PR #8) + incremental audit tables (PR #15) + deterministic legal-form-aware auto-merge code (PR #21) are merged. A **grilling/planning session (2026-07-10)** locked the full **C-end "Recall Profile" feature design** — glossary [CONTEXT.md](CONTEXT.md) + decisions [docs/adr/](docs/adr/) ADR-0001..0004. **The current DB firm state is stale**: it came from **PR #18** (`firm-ai-web-verification`, closed/superseded), a web-first run that stalled on OpenRouter 402 (485 `needs_review`; `parent_group` / `brand_alias` empty). PR #21 improved the resolver in code only: dry-run `src/firm/resolve.py --mode full` yielded 6,848 candidate pairs, 259 accepted, 773 review, 5,816 rejected, 1,442 clusters, with zero accepted foreign-vs-US legal-form conflicts; no `--apply` or LLM/web verification run was executed. The plan is to rebuild the firm layer deterministically on `main`, not merge/salvage the web-first DB state. Details in Next up item 1.
- ▶️ **Next actions (parallel queue, coordinator-owned):**
  - **Firm track:** build **3a+ incremental company-name normalization** before 3b Agent wiring: run/pair audit, incremental `src/firm/resolve.py` after `fetch_openfda.py --since auto`, threshold calibration, and review logs for ambiguous/unknown firms.
  - **Backend/RAG track:** OpenRouter query-embedding support (#39) restores true hybrid retrieval when `EMBED_PROVIDER=openrouter`; continue `/ask` agent-control polish: domain/meta/out-of-domain guard follow-ups, safe empty-sample policy, conservative semantic rewrite, explicit retrieval-degradation responses, safer FTS fallback ladder, and eval/query_log coverage.
   - ✅ **Taxonomy track:** exact taxonomy `count_by` wiring is merged in PR #25.
  - **Conversation/ops tracks:** server-side conversation context v2, Langfuse L2 if `query_log` shows a concrete gap, and optional public deploy.

## Works now (verified)
1. **Ingest** — [src/fetch_openfda.py](src/fetch_openfda.py): generic openFDA→Postgres, idempotent JSONB upsert, `--since auto` incremental.
2. **Data** — table `drug_enforcement`, 17,723 rows: JSONB `raw` + 23 parsed STORED columns + indexes ([sql/001_parse_drug_enforcement.sql](sql/001_parse_drug_enforcement.sql)).
3. **Schema docs** — verbatim openFDA column comments ([sql/002_drug_enforcement_comments.sql](sql/002_drug_enforcement_comments.sql)).
4. **Analytics engine** — [src/analytics.py](src/analytics.py): `count_total` / `count_by` / `trend` / `sample`, read-only + parameterized, returns evidence `recall_number`s. Draft PR #55 adds raw `recalling_firm` exposure v0: raw-name leaderboard, severity-weighted formula/version, class breakdowns, top reason category, and evidence.
5. **NL→SQL layer** — [src/nl_query.py](src/nl_query.py): question → LLM → validated Pydantic `QuerySpec` (columns/values whitelisted; schema + column comments + value-index injected) → `analytics.py`. All numbers come from SQL. Verified: count_total / count_by / trend / sample.
6. **Serving + UI** — [src/api.py](src/api.py): FastAPI `/ask` (+ `/health`) warms the engine once, returns a chart-friendly, evidence-backed payload, and serves a zero-build ChatGPT-style UI ([web/index.html](web/index.html), [web/app.js](web/app.js), [web/styles.css](web/styles.css)) with a localStorage conversation sidebar, independent sidebar/thread scroll regions, edit-message, stop-generation, scalar / bar / line / table / retrieval / semantic-count rendering, one-time server-generated titles (`POST /title`), and inline-SVG rename/delete controls.
7. **Harness** — [AGENTS.md](AGENTS.md) + auto-generated [PROJECT_INDEX.md](PROJECT_INDEX.md) ([scripts/gen_index.py](scripts/gen_index.py)) + pre-commit hook ([scripts/hooks/pre-commit](scripts/hooks/pre-commit)).
8. **DB** — Postgres.app 17, db `fda`; extensions pgvector 0.8.1 / hypopg 1.4.3 / pg_stat_statements.
9. **Read-only DB MCP** — `postgres-fda` ([.vscode/mcp.json](.vscode/mcp.json), restricted mode).
10. **Skills** — under [.github/skills/](.github/skills/): db-column-docs-from-dictionary, openfda-data-download, skill-writing, learning-session-notes, parallel-agent-pr-coordination, parallel-agent-prompt-pack.
11. **Containerized (local)** — [Dockerfile](Dockerfile) (lean serving image; base-registry + PyPI mirrors are build-args) + [.dockerignore](.dockerignore) + [requirements-serve.txt](requirements-serve.txt). `docker run` serves `/ask` + UI, reaching host Postgres via `host.docker.internal`. Verified.
12. **Path 2 — hybrid retrieval + validated semantic counting (2.1–2.4)** — multi-source `embeddings` table ([sql/003](sql/003_recall_embeddings.sql) + [sql/004](sql/004_embeddings_multisource.sql), keyed `(source, source_id, field)`, every column documented in [sql/005](sql/005_embeddings_comments.sql)) + [src/embed.py](src/embed.py) (`SOURCES` registry; 35,446 vectors) + [src/retrieval.py](src/retrieval.py) (pgvector semantic candidates + Postgres FTS over `content_tsv`, fused with RRF and filter-aware); [src/nl_query.py](src/nl_query.py) routes `semantic_query` (concepts → retrieval, never `ilike`) and can combine it with `count_total` / `count_by`; [src/validation.py](src/validation.py) performs structured LLM yes/no validation with snippets/confidence so semantic counts are returned as estimates with evidence.
13. **Observability + eval (L1)** — [sql/006_query_log.sql](sql/006_query_log.sql) creates the idempotent `query_log` trace table; [src/observability.py](src/observability.py) logs request, QuerySpec, routing decision, compact response metadata, latency, and handled errors for `/ask`; [scripts/run_eval.py](scripts/run_eval.py) runs golden evals from [evals/golden/v1.json](evals/golden/v1.json), now including a semantic-count case.
14. **Phase 4 classification foundation + exact counts** — [sql/007_taxonomy.sql](sql/007_taxonomy.sql) + [src/classify/](src/classify/) provide sidecar taxonomy tables and dry-run-first CLIs for taxonomy induction, closed-set labeling, and residual discovery. PR #23 made closed-set labeling robust/concurrent: out-of-taxonomy LLM node IDs are dropped into `other_reason` instead of crashing, and cache-miss distinct reasons can be labeled concurrently. Taxonomy v1 labeling is populated by the LLM labeler only (21,909 rows, 17,723 records covered; old `rules:v1-prefix` rows deleted) and wired into `/ask` for exact category counts/distributions via PR #25.
15. **Phase 3a firm-resolution foundation** — [sql/008_firm_resolution.sql](sql/008_firm_resolution.sql) + [src/firm/](src/firm/) provide sidecar `parent_group` / `firm` / `firm_alias` / `brand_alias` / `resolution_log` tables and dry-run-first CLIs for firm normalization, candidate generation, provenance, and unknown handling. PR #8 is merged. Not yet wired into `/ask`.
    - `src/firm/resolve.py`: normalizes distinct `drug_enforcement.recalling_firm` strings, uses pg_trgm/token/phonetic candidates, clusters with union-find, optionally verifies pairs with structured LLM output, and writes only with `--apply`.
    - `src/firm/brand.py`: resolves brand/product strings to firm/parent candidates with provenance tiers (`fda_fact`, `inferred_external_or_llm`, `unknown`) and logs unresolved cases instead of fabricating identities.
    - Important limitation: this is a foundation, not the finished production flow. New FDA records can arrive after `fetch_openfda.py --since auto`; the next PR must make firm normalization incremental, idempotent, and auditable per run before the Agent consumes it.
    - **Measured state (2026-07-10, verified in DB) — `run.status=succeeded` ≠ feature done:** `sql/009` audit tables exist and one `--mode full --apply` run is recorded (`firm_resolution_run` id=1). `firm_alias`=1,634 (all raw strings mapped) but `firm`=1,591 → **only ~43 merges auto-applied** (22 pairs `accepted`, 1 `rejected`); **485 candidate pairs stuck in `needs_review`**, and their `resolution_log` rows all failed OpenRouter web verification (`openrouter_web_error … APIStatusError` ×479 = out of AI credit). `parent_group`=**0 rows** and **0 firms have `parent_group_id`** (Parent Group layer unbuilt); `brand_alias`=**0 rows** (Brand entry unbuilt). Many of the 485 are similarity-1.0, obviously-identical pairs (`PFIZER INC`=`Pfizer`, `Akorn Inc`=`Akorn, Inc`) that should merge deterministically with no LLM — auto-merge is too strict. So the C-end "is this company safe?" chain is ~25% built. Design decision recorded in [docs/adr/0001-firm-answers-aggregate-to-parent-group.md](docs/adr/0001-firm-answers-aggregate-to-parent-group.md); domain terms in [CONTEXT.md](CONTEXT.md).

## Next up (ordered)
> Each item lists **concrete deliverables + done-when**. Design rationale lives in [PLAN.md](PLAN.md); don't duplicate it here.

1. **Phase 3a+ — productionize company-name normalization before 3b Agent.**
   - Add a run/audit layer (`sql/009_firm_resolution_runs.sql`): `firm_resolution_run` for each execution and `firm_match_pair` for candidate-pair decisions/signals.
   - Upgrade [src/firm/resolve.py](src/firm/resolve.py) from one-off full resolver to incremental pipeline: `--mode full|incremental`, `--source-table`, `--source-field`, new raw-firm detection, existing alias `record_count` refresh, and retry of prior `unknown` / `needs_review` values.
   - Decision policy: high-confidence deterministic matches may auto-apply; medium-confidence pairs go to review/audit; low-confidence/no-candidate values go to `resolution_log`; optional LLM verification narrows candidates but must not be required for deterministic incremental runs.
   - Add a small public golden set for firm-pair threshold calibration so safe auto-merge thresholds are measured before production use.
   - Recommended production sequence: `fetch_openfda.py --since auto` → apply firm-resolution audit DDL → `src/firm/resolve.py --mode incremental --apply`.
   - **Status (2026-07-13):** `sql/009` is applied and the deterministic resolver code has improved (PR #21 dry-run: 259 accepted / 773 review / 5,816 rejected; zero accepted foreign-vs-US legal-form conflicts), but the DB sidecar still needs a fresh deterministic rebuild on `main`; `parent_group` / `brand_alias` remain unbuilt, and remaining review pairs are not adjudicated.
   - **Done when (checkable, firm-resolution v1):**
     1. **Firm layer:** 0 `needs_review` pairs left unadjudicated — every candidate pair ends `accepted` / `rejected`, and trivially-identical pairs (normalized-equal, e.g. `PFIZER INC`=`Pfizer`) auto-merge deterministically **without** any LLM/web call.
     2. **Parent layer:** `parent_group` populated and the top-100-by-recall firms all have a `parent_group_id`; `Hospira Inc.` resolves to Pfizer; every firm→parent edge carries `provenance_tier` + `source` + as-of date.
     3. **Honesty:** unknown/ambiguous identities stay in `resolution_log` (never fabricated); web/LLM-derived ownership never enters an exact count silently.
     4. **Repeatable:** after `fetch_openfda.py --since auto`, one `--mode incremental` command updates only new/changed firms, refreshes alias `record_count`, and is idempotent on re-run.
   - **C-end "Recall Profile" feature — design locked in the 2026-07-10 grilling session (current top priority).** Decisions live in [CONTEXT.md](CONTEXT.md) + [docs/adr/](docs/adr/) 0001–0004 (do not restate here). In short: aggregate to Parent Group but render **two blocks** (FDA-fact firm core + labeled `[inferred]` parent rollup); firm = legal entity with jurisdiction-aware merge (US `Inc`/`Corp`/`LLC`/`LP` merge, foreign `Ltd`/`GmbH`/`Pvt` stay separate); parent edges require a citable structured source + human confirm (Wikidata; LLM/web propose-only); answer is a **Recall Profile, never a safety verdict/score**; v1 wiring = a deterministic **`firm_profile` QuerySpec intent** (the Phase 3b agent in item 4 replaces it later); brand entry (`Tylenol`→firm) = milestone 2.
     - **Walking-skeleton done-def:** `"is Pfizer safe?"` in the UI returns a two-block card (fact-block counts + citation-backed inferred rollup with a `Pfizer`←`Hospira`/`King`/`Meridian` hand seed + rule-table takeaway + evidence drill, no verdict) + a golden eval pinning counts/takeaway category and asserting no safe/unsafe verdict.
   - **Open-PR disposition (verified 2026-07-12):** **PR #18** (`firm-ai-web-verification`) is **superseded** — do not merge; salvage `web_verify.py` later as optional non-blocking enrichment. **PR #17** (`taxonomy-exact-count-wiring`, the deferred "召回原因 Top-N") was closed as **superseded** by PR #25 because it was stale/dirty against `main` and pinned obsolete rules-labeler counts.
2. **Backend/RAG control layer — make `/ask` an agent, not a raw query box.**
   - ✅ **Done (2026-07-09):** first-stage **LLM intent gate** before `QuerySpec` ([src/agent_control.py](src/agent_control.py) `classify_llm`): language-agnostic `in_domain` / `chitchat_meta` / `out_of_domain` / `ambiguous`, user-language messages, no rule fallback (the rule-based classifier was deleted); golden `ask` cases now exercise it (English + Chinese meta/out-of-domain). This fixes the prior behavior where brittle English-only rules blocked legitimate non-English in-domain questions (e.g. “有多少药品因为药效太强被召回”) as `ambiguous` in ~0ms.
   - **Done (2026-07-13, PR #34):** taxonomy/concept meaning questions now have a dedicated explanation route so cGMP/GMP category questions return a plain-language `taxonomy_explanation`; explicit count/top/distribution/by-firm wording still routes to SQL charts/counts.
   - **Done (2026-07-13, PR #36):** firm-scoped top-N questions asking for both recall reasons and products now return a `multi_section` answer with separate taxonomy-category and product tables; `query_log` metadata records section counts/specs for auditability.
   - Add a first-stage guard before `QuerySpec`: classify prompts as `in_domain`, `chitchat/meta`, `out_of_domain`, or `ambiguous`.
   - `who are you?` / capability questions should return a system self-description; out-of-domain prompts should explain FDA recall scope; ambiguous in-domain prompts should ask for clarification.
   - Prevent `intent=sample` with no `filters` and no `semantic_query` from returning arbitrary `drug_enforcement LIMIT 5` rows.
   - Keep semantic rewrites conservative for count/summary questions: preserve the core concept (`sterility`) and keep aliases/expansions separate instead of narrowing to phrases such as `sterility problems`.
   - If embeddings are unavailable, mark answers and `query_log` as degraded `fts_only`; zero FTS hits under degraded mode must not be presented as “FDA has no matching recalls.”
   - Add golden evals for meta/chitchat, out-of-domain, empty-sample prevention, conservative rewrite, and degraded retrieval metadata.
   - **Done when:** `who you are?` does not touch DB; `How many sterility recalls...` does not rewrite to a narrower query; `pills too strong` either uses vector/hybrid or returns an explicit degraded-mode explanation instead of silent 0.
3. ✅ **Phase 4 — exact taxonomy counts.**
   - ✅ Taxonomy v1 labels are populated and cleaned up: only `llm:deepseek/deepseek-v4-pro` remains for `version='v1'`.
   - ✅ Merged in PR #25: `GROUP BY recall_label` in [src/analytics.py](src/analytics.py) / [src/nl_query.py](src/nl_query.py) so “sterility by firm” returns exact counts for known categories.
   - ✅ Superseded PR #17 is closed.
   - **Done:** a `count_by` over a taxonomy node works end-to-end in `/ask`.
4. **Phase 3b — tool-calling Agent (north-star; replaces the interim `firm_profile` intent from item 1).**
   - After the C-end `firm_profile` skeleton ships, add the OpenAI function-calling agent over analytics/retrieval/firm tools so routing is agentic instead of a fixed intent enum.
   - **Done when:** “is <brand> safe?” resolves brand→firm set and returns an evidence-backed Recall Profile with provenance tiers.
5. **Conversational context v2.** Server-side conversation history keyed to `query_log`; `/ask` accepts a conversation id + prior turns so follow-ups carry context. **Done when:** a follow-up question resolves pronouns/ellipsis using prior turns.
6. **Observability L2 — Langfuse.** Only after L1 `query_log` shows a concrete gap. Wrap the `/ask` LLM calls as Langfuse traces/spans; keep `query_log` as the SQL-queryable source of truth. **Done when:** a traced `/ask` is inspectable in Langfuse without losing the `query_log` row.
7. **Public deploy (optional).** Build/push the image to HF Spaces or Render + a managed Postgres with pgvector (Supabase / Neon); load `sql/` + embeddings; set `DATABASE_URL` / `OPENAI_API_KEY` / `OPENROUTER_API_KEY` as secrets. **Done when:** a public URL serves `/ask` against the managed DB.

## Backlog (unscheduled ideas)
- **Company Exposure Index** — per-product-category ranking of firms/brands by openFDA "exposure" (à la Karpathy's AI-exposure), with a category dropdown → ranked chart. **Raw v0 is in draft PR #55** at FDA `recalling_firm` string level only; the full showcase remains coupled to Phase 3 entity resolution for correct parent-group/brand rollups. Key future nuance: exposure ≠ raw count → add normalization (per product/NDC count) + recency + category slicing after the raw severity-weighted v0.

## Blockers & gotchas
- ⚠️ **The venv is not relocatable** — it broke once after the project folder was renamed; recreated. Always run `.venv/bin/python …`, or re-`source .venv/bin/activate` after any move.
- ⚠️ **`hypopg` is built into the Postgres.app bundle** — rebuild it after a Postgres.app major-version upgrade.
- ⚠️ **Postgres MCP quirks** — `get_object_details` does not render column comments, and restricted-mode `execute_sql` rejects catalog queries (`col_description`, `::regclass`). Read comments via `psql \d+` or a direct connection; normal data `SELECT`s through the MCP are fine.
- ℹ️ **`state` stores 2-letter codes** (`CA`, not `California`). The NL layer enumerates a column's allowed values into the prompt ("value index") so the LLM uses real codes — keep that pattern when adding categorical filters.
- 🐳 **Docker build on this machine (CN network):** Docker Hub + PyPI time out — build with mirror build-args (`REGISTRY=docker.m.daocloud.io`, `PIP_INDEX_URL=https://pypi.tuna.tsinghua.edu.cn/simple`). Don't hand-edit `~/.docker/daemon.json` (Docker Desktop overwrites it); the base-image registry is parameterized in the [Dockerfile](Dockerfile) instead.
- 🐳 **Container → host Postgres:** use `DATABASE_URL=postgresql://<role>@host.docker.internal:5432/fda`. `host.docker.internal` reaches Postgres.app even though it only listens on `localhost`, but you MUST set the role — the container runs as `root` (no such Postgres role); the host role is `waywei`.
- 🔑 **OpenAI quota:** keep serving with `LLM_PROVIDER=openrouter` while the OpenAI account returns `insufficient_quota` for chat/title/validation. Query embeddings may also use `EMBED_PROVIDER=openrouter`, but only with the dimension-compatible `openai/text-embedding-3-small` model slug.
- 🧭 **Agent-control gap:** `query_log` examples showed two current failure modes: meta prompts can be forced into `sample`, and embedding failures can degrade semantic phrases to brittle FTS-only zero results. Until the guard layer lands, inspect `query_log.retrieval_mode`, `embedding_fallback_reason`, empty `sample` specs, and suspicious semantic rewrites before trusting “no result.”
- 🧯 **Long-running CLI jobs:** don't send repeated progress probes into the same persistent shell while a long labeling/ingest job is running; a command backlog once desynced the terminal even though Postgres was healthy. Monitor long jobs through fresh shells or read-only DB queries with timeouts.

## Decisions (settled — don't re-litigate)
- **Dataset = `drug/enforcement`** — right-sized (~17.7k), clean, single-file download.
- **Storage = JSONB `raw` + STORED generated columns** — parsed columns auto-recompute on re-ingest, so they never drift from `raw`.
- **Deterministic engine first, LLM on top** — every number comes from SQL, never from the model.
- **One store = Postgres + pgvector** — no separate vector DB.
- **MCP is read-only; schema changes go through versioned `sql/` scripts**, not ad-hoc writes.
- **Column docs = verbatim openFDA text**; anything not from openFDA is marked `[inferred]`.
- **Path 2 retrieval = hybrid, in Postgres** — pgvector (semantic) ⊕ Postgres FTS `ts_rank` (keyword) fused via RRF. True BM25 (`pg_search`) only if FTS recall proves insufficient.
- **Embeddings = `text-embedding-3-small` (1536-d)** in a separate, **multi-source** `embeddings` table keyed `(source, source_id, field)` — one row per (record, field), e.g. `drug_enforcement` × {reason_for_recall, product_description} — NOT extra columns on the source table. One HNSW + one GIN index cover all sources/fields; adding an FDA dataset = one `SOURCES` entry in `embed.py` (no schema change). Incremental re-embed only new/changed text.
- **Concepts route via `semantic_query`, never `ilike`** — hard facts go in `filters` (Tier-A columns), fuzzy concepts in `semantic_query`.
- **Semantic counting is an estimate** — retrieve-above-threshold → per-item validate → count + confidence/evidence. Exact counts should come from taxonomy labels once Phase 4 is populated.
- **Agent ≠ query pipeline** — `/ask` must first decide whether a prompt should query FDA data at all. Meta/chitchat/capability questions answer directly; out-of-domain prompts explain scope; ambiguous prompts ask clarification; empty-filter `sample` must not silently query random rows.
- **No silent retrieval degradation** — vector+FTS hybrid, FTS-only fallback, and SQL keyword search are different evidence regimes. If embeddings fail, the response/metadata must say “degraded FTS-only”; zero FTS hits under degraded mode must not be interpreted as “FDA has no matching recalls.”
- **Provider boundary:** chat / structured-output LLM calls may move to OpenRouter first (`LLM_PROVIDER=openrouter`, OpenRouter model slugs, `OPENROUTER_API_KEY`); embeddings remain a separate provider/model boundary because stored pgvector rows depend on the original embedding dimension/space. Current stored rows are `text-embedding-3-small` 1536-d; `EMBED_PROVIDER=openrouter` uses the compatible OpenRouter model slug `openai/text-embedding-3-small` so semantic queries can return `retrieval_mode=hybrid` without re-embedding the corpus. If embeddings are unavailable, prefer explicit FTS-only fallback over silent bad vectors.
- **Observability before scale** — `query_log` (Postgres, L1) first, then Langfuse (L2); every `/ask` is a trace and the `QuerySpec` is the materialized, inspectable "reasoning".
- **"Is this company safe?" = Phase 3 capstone, after Path 2** — brand→parent is `[inferred]` (LLM / Wikidata / NDC labeler, marked + confirmable); company→`recalling_firm` is entity resolution (`pg_trgm` fuzzy + known-subsidiary expansion + LLM verify), since firms are fragmented (1,634 distinct; Pfizer/Teva/McNeil appear under many names/subsidiaries).
- **Vision = FDA-grounded vertical agent** (not just NL→DB): FDA = ground-truth fact layer; web/Wikidata = augmentation layer, strictly isolated (web numbers never enter counts; negative = "not found in FDA", not "safe"). Two scenarios: personal pull (Q&A) + enterprise push (monitor watched firms/device types; reuses `--since auto`).
- **Automated classification (Phase 4) = induce + closed-set label + discovery loop; human governs, never hand-labels** (TnT-LLM). Closed-set classifier assigns known taxonomy; open-set residual clustering surfaces new categories for approval. Initial taxonomy is data-induced (cluster + prefix mining), not hand-written. Label distinct text only; offline + hash-cached; optional distilled classifier.
- **Entity resolution: recall via multi-signal union (not normalization alone), precision via verify** — identity ≠ FDA footprint (`fda_present`; external/zero-recall firms exist; truly-unknown → `resolution_log`, never fabricate). NDC = 18% verifier, names = 100% path. Tables are a versioned side-car; source table stays immutable.
- **Firm normalization must be incremental in production** — openFDA data changes via `fetch_openfda.py --since auto`; entity-resolution cannot be a one-time batch. Each run should process new/changed `recalling_firm` values, update alias counts/evidence, record candidate-pair decisions with a run id, and leave ambiguous/unknown cases reviewable before 3b Agent uses the sidecar.
- **Cosmetics ≈ out of scope** — openFDA has cosmetic *adverse events* (`/food/event`), not a clean recall endpoint; the architecture generalizes to `device`/`food/enforcement` (same firm structure).
